import type { ChatMessage } from "../types/chat-message";

export const initialMessages: ChatMessage[] = [
  {
    id: 1,
    role: "assistant",
    text: "Ask about uploaded bills, receipts, vendors, dates, or totals. This preview uses sample responses only.",
  },
];
